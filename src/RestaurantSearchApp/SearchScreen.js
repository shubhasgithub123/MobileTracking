import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import SearchBar from './component/SearchBar'

const SearchScreen = () => {
  return (
    <View >
        <SearchBar />
      <Text>SearchScren hai </Text>
    </View>
  )
}

export default SearchScreen

const styles = StyleSheet.create({})